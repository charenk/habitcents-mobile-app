# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native builds
ios/
android/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# TypeScript
*.tsbuildinfo

# Metro
.metro-health-check*

# Claude Code: local settings and worktrees stay out, but .claude/skills/ is
# checked in so every session and scheduled routine loads them cold.
.claude/*
!.claude/skills/

# Leak Scan eval harness: Charen's sanitized real bank exports for local runs
# only. Never committed (see __tests__/leakScanEval/README.md).
__tests__/leakScanEval/private/

# node_modules as a symlink (worktree setups); the trailing-slash pattern above misses links
node_modules
